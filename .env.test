# Test Database Configuration
DATABASE_URL="postgresql://blog_user:blog_password@localhost:5433/blog_test_db"

# Ensure this is clearly a test environment
NODE_ENV=test

# Redis disabled for tests (uses in-memory cache)
REDIS_HOST=
REDIS_PORT=
REDIS_PASSWORD=
REDIS_DB=

# Logging Configuration - silent during tests
LOG_LEVEL=error
